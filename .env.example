# -----------------------------------------------------------------------------
# Rising Kashmir — demo authentication (optional)
# Copy to ".env.local" for local sign-in. Never commit .env.local.
#
# Demo password used to generate the sample HASH/SALT below:
#   RisingKashmir2026!
# Regenerate anytime: node scripts/hash-password.mjs "YourNewPassword"
# -----------------------------------------------------------------------------

RK_AUTH_SECRET=gSYdVR6HLKJtv91snmgOuxxIGxAneLtrNIe7jKC9hOo
RK_DEMO_USER_EMAIL=demo@risingkashmir.local
RK_DEMO_PASSWORD_SALT=a053dbc7e13ae27be734772c66356bf3
RK_DEMO_PASSWORD_HASH=ae72a9b0c3cf1304cdd4fa60b4295818052ed1889e444682232b762d183bf398864b0a856e8e4b3b7c736991e29c9ac451ae09d1461c14b0a87c637375a1e8e6

# Production: replace demo block with OAuth (e.g. Auth.js) and remove demo vars.
#
# Deploy (sample): Vercel — import repo, set these env vars in Project → Settings → Environment Variables.
# Render — use render.yaml or create a Web Service; same env vars. Docker — see Dockerfile (standalone).
# Optional: REVALIDATE_SECRET for POST /api/revalidate (CMS webhooks).
